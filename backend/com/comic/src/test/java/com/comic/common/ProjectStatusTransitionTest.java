package com.comic.common;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * 原测试依赖已移除的 {@code ProjectStatus}。
 * 状态迁移请针对 {@link com.comic.statemachine} 编写新用例。
 */
@Disabled("Obsolete: ProjectStatus removed; add ProjectMilestone / state machine tests separately")
class ProjectStatusTransitionTest {

    @Test
    void placeholder() {
        // suite disabled at class level
    }
}
